/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		unoptimized: false,
		domains: ["image.tmdb.org"],
	},
};

export default nextConfig;

// images: {
// 	domains: ["image.tmdb.org"],
// },
